export type SendData = {
    message: any,
    applyData: string,
    token: string,
    roomId: string
}

export type messageSave = {
    message: any,
    username: string,
    accountId: number,
    timeSent: number
}